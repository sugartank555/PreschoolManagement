@model Tuple<string,string,string> 
<div class="card shadow-sm h-100">
    <div class="card-body d-flex align-items-center">
        <div class="me-3">
            <div class="bg-primary bg-opacity-10 text-primary rounded-3 d-inline-flex align-items-center justify-content-center" style="width:44px;height:44px">
                <i class="fa @Model.Item3"></i>
            </div>
        </div>
        <div>
            <div class="text-muted small">@Model.Item1</div>
            <div class="fs-4 fw-semibold">@Model.Item2</div>
        </div>
    </div>
</div>
